enum BlocStatus { initial, loading, success, failure }

enum SortingDirection { asc, desc }

enum QbLink { or, and }

// ignore: constant_identifier_names
enum PrefsKeys { theme_mode, current_tab, seen_onboarding, last_auth_user }

enum SCKeys { newEntry, themeSwitcher, profileBtn, balanceCard }
