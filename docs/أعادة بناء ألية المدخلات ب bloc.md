## مدخلات اليوم
- هل عرض كل مدخلات اليوم مناسب ؟ ام يفضل إستخدام pagination ؟
	- لا يوجد سبب للتصفح في رأيي ، ويفضل جلب كل البيانات
- هل جلب كافة البيانات وتخزينها في ال state مناسب وتصفيتها حسب الحاجة بعد ذلك؟ أم يكفي جلبها عند الحاجة من قاعدة البيانات ؟
	- يكفي جلب البيانات من قاعدة البيانات عند الحاجة بدل عمل لغة ودوال جديدة لتصفية البيانات مع وجود sql أو ال query builder

